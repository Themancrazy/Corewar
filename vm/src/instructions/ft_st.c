#include "vm.h"

void				ft_st(t_cw *cw, t_process **cp)
{
    // (void)cw;
    // (void)cp;
    // t_process   *tcp;
    // uint32_t    value; // VALUE CAN BE MODIFIED (SIZEOF(TYPE))
    // //uint32_t    offset;

    // tcp = *cp;
    // value = tcp->registers[4];
    // offset = *(tcp->pc + 2);
    // // (tcp->pc + offset) = value; //WRITE VALUE AT (PC + OFFSET) IN MEMORY
    // printf("st called\n");
}